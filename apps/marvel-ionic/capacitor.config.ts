import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pmt.marvelgoodies',
  appName: 'Marvel Goodies',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
